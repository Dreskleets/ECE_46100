import sqlite3
#con = sqlite3.connect("review_data.db")
#cur = con.cursor()

#Database Layout
    #SQL Lite
        #Each AI has individual tables
            #ID (Secure Key)
            #Ratings
            #Review
            #Runtime (Avg)
